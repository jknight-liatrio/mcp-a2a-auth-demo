apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - keycloak.yaml

configMapGenerator:
  - name: spire-server-ca
    files:
      - spire-server-ca-chain.pem

generatorOptions:
  disableNameSuffixHash: true
